# DuckDB Configuration
# Copy this file to .env and adjust values as needed.

# DuckDB CLI version baked into the image (build arg).
DUCKDB_VERSION=1.2.2

# Database file name (stored under ./data on the host,
# which is bind-mounted to /data inside the container).
DUCKDB_DB=app.duckdb
